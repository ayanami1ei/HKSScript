# 代码规范和维护手册

### 命名规范
- 参考C#官方的代码命名规范，所有类、命名空间、函数使用大驼峰，变量使用小驼峰
- 使用翻译标准的英文命名，不会的直接ai

### 长度规范
- 单个函数不得超过100行
- 单个文件不得超过500行

### 文件规范
- 单个文件只能写一个类，如果一个类只在另一个类里使用且代码两较小，可以放在使用它的那个类的文件里
- 按命名空间分文件夹

### 面向对象规范
- 所有代码，除了少数对外接口，必须完全面向对象
- 尽量不使用静态函数
- 牢记单一职责，一个类或函数只能实现一个功能
- 类的职责优先从数据的角度考虑，对象从数据开始；找不到数据或根据类按函数划分职责
- 减少继承，多用组合

### 使用Git管理项目
- 完成一部分工作或定时提交更改
```
git add . #提交更改
git commit -m "<描述>" #提交更改的描述
```
- 活用分支，每次增加功能都新建分支，不满意删除即可
```
git checkout -b <分支名> #创建并转到新分支
git checkout <分支名> #转到已有分支，代码会一同变到该分支的代码

#合并分支，将子分支并入母分支
git checkout <母分支名>
git merge <子分支名>

git branch -d <分支名> #删除分支
```
- 使用远程仓库
```
git remote add <仓库名> <仓库url> #增加远程仓库
git push <仓库名> <分支名> #将分支推送到远端
git pull <仓库名> <分支名> #将远端的分支拉取到本地 
```