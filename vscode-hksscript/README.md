# HksScript - VS Code Extension

HksScript 语言支持：语法高亮 + 基本的语法检查。

## 功能

- **语法高亮**：关键字、类型、字面量、运算符、注释
- **语法检查**：基于 ANTLR 解析器，实时检测语法错误
- **语言配置**：代码折叠、括号匹配、自动闭合

## 使用

安装后在 `.hks` 文件中自动激活。

## 开发

```bash
npm install
npm run compile
```

在 VS Code 中按 F5 启动 Extension Development Host 进行调试。
